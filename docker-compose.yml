version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: bnbgest-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: bnbgest
      POSTGRES_PASSWORD: bnbgest123
      POSTGRES_DB: bnbgest
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bnbgest"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
